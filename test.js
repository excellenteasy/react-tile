'use strict'
const React = require('react')
const test = require('tape')
import {TextCell} from './'

test('TextCell spec', function (t) {
  t.plan(1)
  t.ok(true, 'Sorry, no tests yet :( Let them pass for semantic-release to work.')
})
